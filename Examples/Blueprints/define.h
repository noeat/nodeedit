#pragma once

enum COMMANDTYPE
{
	NONE,
	STARTUP,
	SETTING,
};

enum NODETYPE
{
	NONE,
	ENTRY,
	SEQUENCE = 100,
	PARALLEL = 101,
	BRANCH = 103,
	LISTEN = 104,
	REPEATED = 200,
	WAIT = 201,
	EVENTWAIT = 202,
	EVENTADPTOR = 203,
	ACTION = 300,
};